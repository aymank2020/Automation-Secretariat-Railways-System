# === للتشغيل المحلي (SQLite) - لا يحتاج PostgreSQL ===
DATABASE_URL=sqlite:///./railways_hr.db

# === للتشغيل مع PostgreSQL (Docker) ===
# DATABASE_URL=postgresql://railways_user:railways_password@localhost:5432/railways_hr

# === للتشغيل مع Docker Compose ===
# DATABASE_URL=postgresql://railways_user:railways_password@postgres:5432/railways_hr

SECRET_KEY=22121990
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
UPLOAD_DIR=./uploads
