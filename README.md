# نظام إدارة المراسلات - السكك الحديدية 🚂📝

نظام متكامل لإدارة المراسلات الواردة والصادرة للسكك الحديدية.

## 📋 المميزات
- ✅ إدارة المراسلات الواردة والصادرة
- ✅ نظام صلاحيات (مدير/مستخدم)
- ✅ البحث المتقدم
- ✅ سجل التعديلات
- ✅ واجهة عربية RTL
- ✅ تصميم متجاوب

## 🛠 التقنيات
- **Backend:** FastAPI + SQLite/PostgreSQL + SQLAlchemy
- **Frontend:** Vue 3 + Vite + Tailwind CSS

## 🚀 التشغيل

### 1. Backend
```bash
cd backend
pip install -r requirements.txt
copy .env.example .env
uvicorn app.main:app --reload
```

السيرفر يشتغل على: http://localhost:8000

> ملاحظة: قاعدة البيانات (SQLite) تتنشأ تلقائياً + المستخدمين الافتراضيين يتنشأوا تلقائياً عند أول تشغيل.

### 2. Frontend
```bash
cd frontend
npm install
npm run dev
```

الواجهة تشتغل على: http://localhost:5173

## 🔑 بيانات الدخول
| المستخدم | كلمة المرور | الصلاحية |
|----------|-------------|----------|
| admin    | admin123    | مدير     |
| user     | user123     | مستخدم   |

## 📡 API Documentation
بعد تشغيل الباك إند: http://localhost:8000/docs
