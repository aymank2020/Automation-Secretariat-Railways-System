# نظام إدارة المراسلات - السكك الحديدية 🚂📝

نظام متكامل لإدارة المراسلات الواردة والصادرة للنقابات العمالية للسكك الحديدية.

## 📋 المميزات

- ✅ **إدارة المراسلات**: تسجيل وتتبع المراسلات الواردة والصادرة
- ✅ **الصلاحيات**: نظام صلاحيات (مدير/مستخدم)
- ✅ **البحث المتقدم**: بحث في جميع بيانات المراسلات
- ✅ **رفع الملفات**: إمكانية إرفاق ملفات (PDF، صور)
- ✅ **واجهة عربية**: واجهة مستخدم كاملة بالعربية RTL
- ✅ **Responsive**: تصميم متجاوب يعمل على الجوال والكمبيوتر
- ✅ **سجل التعديلات**: تتبع كامل للتغييرات وتعديلات المراسلات
- ✅ **Docker**: سهولة التثبيت والنشر باستخدام Docker

## 🛠 التقنيات المستخدمة

### الباك إند (Backend)
- **FastAPI** - إطار عمل Python حديث وسريع
- **PostgreSQL** - قاعدة بيانات احترافية
- **SQLAlchemy** - ORM لإدارة قاعدة البيانات
- **JWT** - للمصادقة والأمان

### الفرونت إند (Frontend)
- **Vue 3** - إطار عمل JavaScript حديث
- **Vite** - أداة بناء سريعة
- **Tailwind CSS** - إطار عمل CSS حديث
- **Pinia** - إدارة الحالة (State Management)
- **Axios** - للتعامل مع APIs

## 📦 التثبيت والتشغيل

### المتطلبات الأساسية

- **Docker** و **Docker Compose** (موصى به)
- أو **Python 3.10+** + **Node.js 18+**

### الطريقة الأولى: باستخدام Docker (موصى به) 🐳

1. **استنساخ المشروع**
```bash
git clone https://github.com/aymanbayoumi080-oss/Automation-Secretariat-Railways-System.g.git
cd Automation-Secretariat-Railways-System
```

2. **إنشاء ملف البيئة للباك إند**
```bash
cd backend
cp .env.example .env
```

3. **تعديل ملف `.env`**
```bash
# داتابيز
DATABASE_URL=postgresql://postgres:postgres@db:5432/railways_db
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
UPLOAD_DIR=/app/uploads
```

4. **تشغيل المشروع**
```bash
cd ..
docker-compose up -d
```

5. **إنشاء قاعدة البيانات**
```bash
docker-compose exec backend python seed_db.py
```

6. **فتح النظام**
   - الباك إند: http://localhost:8000
   - الفرونت إ الند: http://localhost:5173
   - API Documentation: http://localhost:8000/docs

### الطريقة الثانية: التشغيل المباشر ⚙️

#### تشغيل الباك إند

1. **تثبيت Python Dependencies**
```bash
cd backend
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install -r requirements.txt
```

2. **إنشاء ملف البيئة**
```bash
cp .env.example .env
```

3. **تعديل ملف `.env`**
```bash
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/railways_db
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
UPLOAD_DIR=./uploads
```

4. **تشغيل قاعدة البيانات (PostgreSQL)**
   - تأكد من تثبيت PostgreSQL وتشغيله
   - أنشئ قاعدة بيانات جديدة باسم `railways_db`

5. **إنشاء الجداول والبيانات الأولية**
```bash
python seed_db.py
```

6. **تشغيل الباك إند**
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### تشغيل الفرونت إند

1. **تثبيت Node Dependencies**
```bash
cd frontend
npm install
```

2. **إنشاء ملف البيئة**
```bash
echo "VITE_API_URL=http://localhost:8000" > .env
```

3. **تشغيل الخادم التطويري**
```bash
npm run dev
```

4. **فتح المتصفح**
   - افتح http://localhost:5173

## 👥 بيانات الدخول الافتراضية

### مدير النظام
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`

### مستخدم عادي
- **اسم المستخدم**: `user`
- **كلمة المرور**: `user123`

> ⚠️ **مهم**: قم بتغيير كلمات المرور هذه في بيئة الإنتاج!

## 📂 هيكل المشروع

```
Automation-Secretariat-Railways-System/
├── backend/                 # الباك إند (FastAPI)
│   ├── app/
│   │   ├── api/            # مسارات API
│   │   │   ├── auth.py     # المصادقة
│   │   │   ├── users.py    # إدارة المستخدمين
│   │   │   └── documents.py # إدارة المراسلات
│   │   ├── core/           # التكوين والأمان
│   │   ├── db/             # قاعدة البيانات
│   │   ├── models/         # نماذج قاعدة البيانات
│   │   └── schemas/        # Pydantic Schemas
│   ├── Dockerfile
│   ├── .env.example
│   └── requirements.txt
├── frontend/               # الفرونت إند (Vue 3)
│   ├── src/
│   │   ├── components/     # المكونات
│   │   ├── pages/          # الصفحات
│   │   ├── router/         # التنقل
│   │   ├── stores/         # Pinia Stores
│   │   └── services/       # API Services
│   ├── package.json
│   └── vite.config.js
├── docker-compose.yml      # Docker Configuration
└── README.md               # هذا الملف
```

## 🔧 الإعدادات المتقدمة

### تغيير المنفذ

**الباك إند:**
```bash
# في docker-compose.yml
ports:
  - "8001:8000"  # بدل 8000
```

**الفرونت إند:**
```bash
# في docker-compose.yml
ports:
  - "5174:5173"  # بدل 5173
```

### تغيير السكرت كي (SECRET_KEY)

مهم جدًا في بيئة الإنتاج لتأمين JWT tokens:

```bash
# في backend/.env
SECRET_KEY=$(openssl rand -hex 32)
```

## 🚀 النشر على سيرفر (Deployment)

### خطوات النشر على VPS

1. **تثبيت Docker على السيرفر**
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
```

2. **استنساخ المشروع**
```bash
git clone https://github.com/aymanbayoumi080-oss/Automation-Secretariat-Railways-System.g.git
cd Automation-Secretariat-Railways-System
```

3. **تعديل الإعدادات**
```bash
cd backend
cp .env.example .env
# عدّل .env بالسكرت كي الجديد والإعدادات
```

4. **تشغيل الـ Containers**
```bash
cd ..
docker-compose up -d --build
```

5. **إعداد Nginx (اختياري)**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:5173;
    }

    location /api {
        proxy_pass http://localhost:8000;
    }
}
```

6. **تمكين HTTPS باستخدام Let's Encrypt**
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

## 🐛 حل المشاكل الشائعة

### خطأ "Database connection failed"
- تأكد من أن PostgreSQL يعمل
- تحقق من صحة `DATABASE_URL` في ملف `.env`
- إذا تستخدم Docker، تأكد من أن container قاعدة البيانات يعمل

### خطأ "Cannot connect to backend"
- تأكد من أن الباك إند يعمل على المنفذ 8000
- تحقق من `VITE_API_URL` في الفرونت إند `.env`

### مشاكل رفع الملفات
- تأكد من صلاحيات مجلد `uploads`
- تحقق من حجم الملف (الحد الأقصى 5MB)

## 📝 تطوير النظام

### إضافة API Endpoint جديد

1. أنشئ ملف جديد في `backend/app/api/`
2. أضف الـ router في `backend/app/main.py`
3. أنشئ Pydantic schema في `backend/app/schemas/`

### إضافة صفحة جديدة في الفرونت إند

1. أنشئ ملف `.vue` في `frontend/src/pages/`
2. أضف الـ route في `frontend/src/router/index.js`
3. أضف رابط في `Sidebar.vue`

## 🤝 المساهمة

 المساهمات مرحب بها! إذا أردت المساهمة:

1. Fork المشروع
2. أنشئ فرع جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

## 📜 الترخيص

هذا المشروع مفتوح المصدر ومتاح تحت رخصة MIT.

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع قسم "حل المشاكل الشائعة"
2. افتح Issue على GitHub
3. تواصل مع المطور

## 🙏 شكر وتقدير

- شكرًا لاستخدام نظام إدارة المراسلات للسكك الحديدية!
- تم تطويره 🇪🇬 في مصر ❤️

---

**صمم وطور بـ ❤️ لخدمة السكك الحديدية**